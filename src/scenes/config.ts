// Список миров = папки public/assets/worlds/<имя>/ с meta.json внутри.
// Клавиши 1..9 — прямой выбор, W — следующий, M — предыдущий.
export const SCENE_CONFIG = {
  worlds: ['living', 'bedroom', 'balcony', 'demo-splat'],
}
